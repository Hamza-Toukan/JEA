/**
 * @deprecated Import from `./request` or domain services directly.
 * Thin re-export for backward compatibility.
 */
export { api, get, post, patch, del as delete, request, buildUrl } from "./request";
